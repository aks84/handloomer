%h1 Listing products

.sort_products
  %p Sort Products
  = select_tag :"category", options_for_select(Category.all.collect{|c| [c.name, c.id]})

- @products.each do |product|
  .product_list
    %h2= link_to product.name, product
    .post_imgs
      = image_tag product.images_urls(:card)[0]
    %small.cat
      Category - #{product.category.name}
    %p= truncate(product.desc, length: 75)

    = link_to "- Learn More -", product, class: "contact-btn"


